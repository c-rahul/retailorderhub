if ([string]::IsNullOrWhiteSpace($env:SONAR_TOKEN)) {
	throw "SONAR_TOKEN is not set. Configure it in the current PowerShell session before running the scan."
}

mvn clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:5.8.0.7211:sonar "-Dsonar.projectKey=rahul-chattopadhyay_retailorderhub" "-Dsonar.organization=rahul-chattopadhyay" "-Dsonar.host.url=https://sonarcloud.io" "-Dsonar.token=$env:SONAR_TOKEN"